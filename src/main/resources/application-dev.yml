spring:
  data:
    mongodb:
      uri: mongodb+srv://${MONGODB_USERNAME}:${MONGODB_PASSWORD}@${MONGODB_CLUSTER}/?retryWrites=true&w=majority&appName=Cluster0
      database: journalDB
      auto-index-creation: true
server:
  port: 8080
  servlet:
    context-path: /journalIt